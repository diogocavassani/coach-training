# CoachTraining — orquestração local (PostgreSQL + API .NET + Angular)
# Uso: a partir da pasta infra/
#   docker compose up --build
#
# Variáveis: copie .env.example para .env e ajuste se necessário.

name: coachtraining

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-coach}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-coach}
      POSTGRES_DB: ${POSTGRES_DB:-coachtraining}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$$POSTGRES_USER\" -d \"$$POSTGRES_DB\""]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ..
      dockerfile: src/CoachTraining.Api/Dockerfile
    restart: unless-stopped
    ports:
      - "${API_HTTP_PORT:-8080}:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Development}
      ASPNETCORE_URLS: http://+:8080
      # Deve corresponder a GetConnectionString("DefaultConnection").
      ConnectionStrings__DefaultConnection: >-
        Host=db;Port=5432;Database=${POSTGRES_DB:-coachtraining};Username=${POSTGRES_USER:-coach};Password=${POSTGRES_PASSWORD:-coach}
      Jwt__Issuer: ${JWT_ISSUER:-CoachTraining}
      Jwt__Audience: ${JWT_AUDIENCE:-CoachTraining.Client}
      Jwt__ExpirationHours: ${JWT_EXPIRATION_HOURS:-8}
      Jwt__Key: ${JWT_KEY:-coach-training-local-docker-jwt-key-change-this-2026}
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "${FRONTEND_HTTP_PORT:-4200}:80"

  demoseed:
    build:
      context: ..
      dockerfile: src/CoachTraining.DemoSeed/Dockerfile
    profiles: ["seed"]
    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Development}
      ConnectionStrings__DefaultConnection: >-
        Host=db;Port=5432;Database=${POSTGRES_DB:-coachtraining};Username=${POSTGRES_USER:-coach};Password=${POSTGRES_PASSWORD:-coach}
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
